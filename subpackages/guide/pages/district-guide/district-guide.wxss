page {
  background: #FFFFFF;
}

/* 页面整体容器 */
.container {
  min-height: 100vh;
  background: #FFFFFF;
  padding-bottom: env(safe-area-inset-bottom);
}

/* 顶部标题区 */
.header {
  padding: 48rpx;
  background: #FFFFFF;
}

.district-title {
  font-family: 'PingFang SC';
  font-style: normal;
  font-weight: 500;
  font-size: 48rpx;
  line-height: 100%;
  color: #242529;
}

/* 攻略列表区域 */
.guide-list {
  display: flex;
  flex-direction: column;
  gap: 48rpx;
  padding: 0 48rpx;
}

/* 空状态：当当前区没有攻略数据时，给用户一个明确反馈。 */
.empty-wrap {
  min-height: calc(100vh - 160rpx);
  padding: 64rpx 48rpx 96rpx;
  box-sizing: border-box;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  text-align: center;
}

.empty-illustration {
  width: 176rpx;
  height: 176rpx;
  border-radius: 999rpx;
  background: #F7F8FA;
  display: flex;
  align-items: center;
  justify-content: center;
  margin-bottom: 40rpx;
}

.empty-icon {
  font-size: 80rpx;
  line-height: 80rpx;
  color: #A0A4AE;
}

.empty-title {
  font-family: 'PingFang SC';
  font-weight: 500;
  font-size: 36rpx;
  line-height: 44rpx;
  color: #242529;
}

.empty-desc {
  margin-top: 20rpx;
  font-family: 'PingFang SC';
  font-weight: 400;
  font-size: 26rpx;
  line-height: 38rpx;
  color: #A0A4AE;
}

/* 单张攻略卡片 */
.guide-card {
  width: 100%;
  min-height: 368rpx;
  border-radius: 32rpx;
  padding: 32rpx;
  box-sizing: border-box;
  position: relative;
  background: linear-gradient(180deg, #F7F8FA 0%, #FFFFFF 100%);
}

.guide-cover-wrap {
  position: absolute;
  width: 192rpx;
  height: 192rpx;
  left: 32rpx;
  top: 32rpx;
  border-radius: 16rpx;
  overflow: hidden;
}

.guide-cover {
  width: 100%;
  height: 100%;
  object-fit: cover;
}

.guide-info {
  position: absolute;
  top: 32rpx;
  left: 256rpx;
  right: 32rpx;
  width: auto;
  height: 192rpx;
  display: flex;
  flex-direction: column;
  align-items: flex-start;
  min-width: 0;
}

.guide-title {
  width: 100%;
  max-width: 100%;
  height: 96rpx;
  display: -webkit-box;
  overflow: hidden;
  font-family: 'PingFang SC';
  font-weight: 500;
  font-size: 32rpx;
  line-height: 48rpx;
  color: #242529;
  -webkit-line-clamp: 2;
  -webkit-box-orient: vertical;
  word-break: break-all;
}

.guide-city {
  margin-top: 24rpx;
  font-family: 'PingFang SC';
  font-weight: 400;
  font-size: 24rpx;
  line-height: 100%;
  color: #A0A4AE;
}

.guide-stats {
  margin-top: 16rpx;
  font-family: 'PingFang SC';
  font-weight: 400;
  font-size: 24rpx;
  line-height: 100%;
  color: #A0A4AE;
}

.guide-divider {
  position: absolute;
  width: calc(100% - 64rpx);
  height: 0;
  left: 32rpx;
  top: 272rpx;
  border-top: 2rpx dashed #F7F8FA;
}

.guide-footer {
  position: absolute;
  width: calc(100% - 64rpx);
  left: 32rpx;
  top: 304rpx;
}

.guide-meta {
  display: flex;
  flex-direction: row;
  justify-content: space-between;
  align-items: center;
}

.guide-author-wrap {
  display: flex;
  flex-direction: row;
  align-items: center;
  gap: 8rpx;
}

.guide-author-avatar {
  width: 32rpx;
  height: 32rpx;
  border-radius: 24rpx;
  object-fit: cover;
}

.guide-author {
  font-family: 'PingFang SC';
  font-weight: 400;
  font-size: 24rpx;
  line-height: 100%;
  color: #A0A4AE;
}

.guide-likes {
  font-family: 'PingFang SC';
  font-weight: 400;
  font-size: 24rpx;
  line-height: 100%;
  color: #A0A4AE;
}

/* 列表底部“没有更多了”提示 */
.no-more {
  display: flex;
  flex-direction: row;
  align-items: center;
  justify-content: center;
  padding: 32rpx 32rpx 64rpx;
  gap: 16rpx;
}

.no-more-line {
  width: 160rpx;
  height: 1rpx;
  background: #E7E7E7;
}

.no-more-text {
  font-family: 'PingFang SC';
  font-weight: 400;
  font-size: 24rpx;
  line-height: 100%;
  color: #A0A4AE;
}
